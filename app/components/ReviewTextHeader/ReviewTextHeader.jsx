import React from "react";
import styles from "./ReviewTextHeader.module.css";

function ReviewTextHeader({ user }) {
  return <div className={`${styles.titleHeader}`}>Reviews.</div>;
}

export default ReviewTextHeader;
